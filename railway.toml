[build]
  builder = "nixpacks"
  buildCommand = "npm install -g pnpm@latest && pnpm install --frozen-lockfile && pnpm run build:production"

[deploy]
  startCommand = "node artifacts/api-server/dist/index.mjs"
  restartPolicyType = "on_failure"
  restartPolicyMaxRetries = 3
